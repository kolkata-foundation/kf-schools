import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const examResultImporterSelectors = importerSelectors(
  'examResult.importer',
);

export default examResultImporterSelectors;
