import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const examinationImporterSelectors = importerSelectors(
  'examination.importer',
);

export default examinationImporterSelectors;
