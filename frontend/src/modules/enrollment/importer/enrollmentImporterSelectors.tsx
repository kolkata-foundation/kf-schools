import importerSelectors from 'src/modules/shared/importer/importerSelectors';

const enrollmentImporterSelectors = importerSelectors(
  'enrollment.importer',
);

export default enrollmentImporterSelectors;
